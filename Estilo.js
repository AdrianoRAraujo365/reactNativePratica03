import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#fff'
  },
  FontGrande: {
    fontSize: 28,
    fontWeight: 'bold',
    marginBottom: 20
  },
  FontMedio: {
    fontSize: 20,
    marginBottom: 10
  }
});
